export const metadata = {
  title: "Folder Segregator - Intelligently Organize Your Files",
  description: "Upload a messy folder, let us intelligently categorize the files by their functions, and export a perfectly organized ZIP archive.",
};

import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
